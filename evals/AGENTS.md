# AGENTS.md

Local route card for `aoa-stats/evals/`.

This skeleton port captures future stats eval pressure while preserving the
derived-only posture of `aoa-stats`.

`aoa-evals` owns central verdict, scoring, regression, and proof doctrine
authority. Keep movement summaries derived; route proof adoption to
`aoa-evals`.

Validation:

```bash
python ../aoa-evals/scripts/validate_local_eval_port.py --target-root .
```
