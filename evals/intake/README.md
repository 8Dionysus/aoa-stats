# Intake

Future stats eval pressure will live here as `*.eval_need.json`.

Do not place fake bundles in this skeleton port. Verdict, scoring, regression,
and proof doctrine authority stays in `aoa-evals`.
