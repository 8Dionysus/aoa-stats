# release-support mechanic

`mechanics/release-support/` is the active aoa-stats implementation of the common
`release-support` mechanic.

Publishes release and rollout observability plus artifact-bundle identity without becoming rollout authority.

Start with `PARTS.md`, then use the selected part's `README.md`,
`CONTRACT.md`, and `VALIDATION.md`.
