# Micro-friction receipts

This part owns the stats contracts for bounded micro-friction receipts,
recurrence summaries, and inbox summaries.

Payload lives in `docs/`, `examples/`, `schemas/`, and `tests/`. Start with
`docs/MICRO_FRICTION_RECEIPTS.md`, then validate the three schema/example pairs.
