# Validation

```bash
python -m pytest -q mechanics/experience/parts/adoption-federation-harvest/tests/test_experience_wave3_seed_contracts.py
```

The test validates all 14 local pairs and their guardrail, type, enum, array,
and numeric constraints.
