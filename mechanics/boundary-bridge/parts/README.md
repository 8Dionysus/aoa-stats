# Boundary bridge part routes

Three boundary-bridge parts are active:

- `receipt-abi-crossing/` — shared envelope and event-kind admission
- `consumer-regrounding/` — surface catalog and source-coverage signals that
  point consumers back to stronger sources
- `memory-owner-handoff/` — reviewed memory movement and explicit return to
  aoa-memo

Consult `mechanics/topology.json` for localized paths and justified public
root routes.
