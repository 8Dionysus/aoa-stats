# Agon legacy

This is the historical layer of the Agon stats mechanic. Active operation
contracts are under `../parts/`; the raw landing and quest records remain here
only so their provenance and former routes stay discoverable.

Use `INDEX.md` for human routing and `former-routes.json` for the exact
machine-readable relocation ledger.
