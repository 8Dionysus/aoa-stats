# Agon part routes

The eight active directories below are complete operation homes rather than
labels over root payloads. Each contains its config, documentation, example,
schemas, builder, validator, and focused test.

The only deliberate cross-boundary write is publication of the matching
`generated/agon_*.min.json` compatibility output at repository root.
