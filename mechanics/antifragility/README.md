# antifragility mechanic

`mechanics/antifragility/` is the active aoa-stats implementation of the common
`antifragility` mechanic.

Bounds stress, recovery, negative evidence, and activation pressure without converting derived stats into proof.

Start with `PARTS.md`, then use the selected part's `README.md`,
`CONTRACT.md`, and `VALIDATION.md`.
