# Contract

- Source meaning remains with the exact ATM10-Agent and aoa-evals surfaces
  named in
  `stats/read-models/deferred/antifragility_vector.profile.json`.
- The candidate stays `contract_only` while any authored activation gap is
  open.
- The schema requires receipt, adaptation-delta, and eval refs to remain
  separate. An empty adaptation list is valid and must not be converted into
  inferred adaptation gain.
- Thin current evidence is represented by explicit suppression and null axes,
  not invented scores.
- This part owns its schema, example, docs, and focused validation. It owns no
  runtime source, eval verdict, active read model, routing, gate, identity,
  workflow truth, or MCP mutation.
- Activation must add and review the real producer, public output, active
  profile/slot, current evidence adapter, tests, consumer impact, and any live
  observation route together. Moving the deferred profile is insufficient.
